{{!
  Notification Block
  Params:
    date:   (string) unescaped Date of the current newslatter
}}
{{> ui-fixed-layout-hack tableStart=true }}
  <!-- justia-header-v2 -->
  <table align="center" border="0" cellpadding="0" cellspacing="0" class="justia-header-v2 set-max-width">
    <tr>
      <!-- logo -->
      <td class="logo">
        <!-- logo-img -->
        <a href="{{ default.header.logo.v2.url }}" target="_blank"><img src="{{ default.header.logo.v2.source }}" alt="{{ default.header.logo.v2.alt }}" border="0" class="logo-img" /></a>
      </td>
    </tr>
  </table>
{{> ui-fixed-layout-hack tableEnd=true }}
