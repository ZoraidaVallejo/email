{{! ================================================================================================================= }}
{{! W I D G E T   -   B A N N E R                                                                                     }}
{{! ================================================================================================================= }}

{{!
 * @param {string} imgSrc - Image path (relative or absolute).
 * @param {string} imgAlt - Image alternative text.
 * @param {string} [link] - Optional link for the banner.
 * @param {string} [widgetClass] - Optional extra CSS classes.
}}

{{! ================================================================================================================= }}
{{#table class=(classList "w-face-name" "set-max-width")}}
    <tr>
      <td class="-image" responsive="mobile-padding-horizontal-sides mobile-padding-top mobile-padding-bottom">
      {{>img src=imgSrc alt=mateName}}
      </td>
      <td class="-face-name" responsive="mobile-padding-horizontal-sides mobile-padding-top mobile-padding-bottom">
        <span>{{{mateName}}}</span>
      </td>
    </tr>
{{/table}}