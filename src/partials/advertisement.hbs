<!-- ADVERTISEMENT -->

{{#table class='w-advertisement -client-ad-01 set-max-width'}}
  <tr>
    <td class="ad-image">
      <a href="{{{advertisements.clients.ad1.link}}}" target="_blank">
        <img src="{{resolveFilePath advertisements.clients.ad1.image}}" alt="{{advertisements.clients.ad1.alt}}" border="0" />
      </a>
    </td>
  </tr>

  {{#if advertisements.clients.ad1.description}}
    {{#each advertisements.clients.ad1.description}}
      <tr>
        <td class="ad-desc{{#if @first}} -first{{/if}}{{#if @last}} -last{{/if}}" responsive="mobile-padding-top mobile-padding-horizontal-sides">
          <p>{{{this}}}</p>
        </td>
      </tr>
    {{/each}}
  {{/if}}
{{/table}}
