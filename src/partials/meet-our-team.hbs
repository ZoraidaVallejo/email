<!-- MEET OUR TEAM -->

{{#gt (length meetOurTeam.blocks) 1}}
  {{assign 'meetOurTeamAllClasses' (concat 'w-text-image' 'w-meet-our-team' 'set-max-width' 'multiple' separator=' ')}}
{{else}}
  {{assign 'meetOurTeamAllClasses' (concat 'w-text-image' 'w-meet-our-team' 'set-max-width' separator=' ')}}
{{/gt}}

{{#table class=@root.meetOurTeamAllClasses}}

  {{#gt (length meetOurTeam.blocks) 1}}
    <tr>
      <td class="items-main-title" responsive="mobile-padding-horizontal-sides">
        <h4 class="widget-type-name">MEET OUR TEAM</h4>
      </td>
    </tr>
  {{/gt}}

  <tr>
    <td class="item-list" responsive="mobile-padding-horizontal-sides">
      {{#forEach meetOurTeam.blocks}}
        {{#table align='' class=(concat 'item-' index)}}
          <tr>
            <td class="item-desc" responsive="collapse-one">

              {{#eq (length ../meetOurTeam.blocks) 1}}
                <h4 class="widget-type-name">MEET OUR TEAM</h4>
              {{/eq}}

              <h2 class="widget-main-title">{{{this.title}}}</h2>
              <p>{{{this.description}}}</p>
            </td>
            <td class="item-image" responsive="mobile-hide collapse-one">
              {{#if this.image}}
                <img src="{{resolveFilePath this.image}}" alt="{{this.title}}" border="0" />
              {{else}}
                &nbsp;
              {{/if}}
            </td>
          </tr>
        {{/table}}
      {{/forEach}}
    </td>
  </tr>
{{/table}}
