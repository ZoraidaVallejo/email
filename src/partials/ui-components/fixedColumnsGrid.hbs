{{#if columStart }}
    <td class="grid-row {{ rowClasses }}">
        <table align="center" border="0" cellspacing="0" cellpadding="0" width="{{ fullWidth }}">
            <tr>
                <td align="center" valign="top" width="{{ cellWidth }}">
                    <table cellpadding="0" cellspacing="0" border="0" align="{{ cellAlignment }}" class="grid-cell {{ cellClasses }}">
{{/if}}
{{#if columnDiv }}
        </table>
    </td>
    <td align="center" valign="top" width="{{ cellWidth }}">
        <table cellpadding="0" cellspacing="0" border="0" align="{{ cellAlignment }}" class="grid-cell {{ cellClasses }}">
{{/if}}
{{#if columnEnd }}
                    </table>
                </td>
            </tr>
        </table>
    </td>
{{/if}}
