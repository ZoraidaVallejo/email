{{! ================================================================================================================= }}
{{! T I P   O F   T H E   M O N T H                                                                                   }}
{{! ================================================================================================================= }}

{{!
// TODO: Add anotations.
}}

{{#and widgetConfig.button (compare widgetConfig.button.link "!==" "")}}
  {{assign "widgetWithButton" true}}
{{else}}
  {{assign "widgetWithButton" false}}
{{/and}}

{{! ================================================================================================================= }}

{{#table class="widget w-tip-month set-max-width"}}
  {{>widget-header
    header=widgetConfig.widget.header
    icon=widgetConfig.widget.icon
    link=widgetConfig.widget.link
  }}

  {{!--
  // TODO: Maybe move title and description to a component.
  --}}
  <tr>
    <td class="widget-description{{#unless @root.widgetWithButton}} -no-button{{/unless}}" responsive="mobile-padding-horizontal-sides">

      {{#if widgetConfig.image}}
        {{#table class="text-image-cols"}}
          <tr>
            <td class="content" responsive="mobile-reset-width collapse-one">
              <h3 class="content-title">{{{widgetConfig.title}}}</h3>
              {{#each widgetConfig.description}}
                <p>{{{this}}}</p>
              {{/each}}
            </td>
            <td class="image" responsive="mobile-reset-width mobile-padding-20-top mobile-no-padding-horizontal-sides collapse-one">
              <a href="{{{widgetConfig.image.link}}}" target="_blank">{{>img src=widgetConfig.image.src alt=widgetConfig.image.alt}}</a>
            </td>
          </tr>
        {{/table}}
      {{else}}
        <h3 class="content-title">{{{widgetConfig.title}}}</h3>

          {{#each widgetConfig.description}}
            <p>{{{this}}}</p>
          {{/each}}
      {{/if}}
    </td>
  </tr>

  {{!
  // TODO: Move to `widget-button` component.
  }}
  {{#if @root.widgetWithButton}}
    <tr>
      <td class="widget-button" responsive="mobile-padding-horizontal-sides mobile-align-center">
        {{#uiButton presetStyle=widgetConfig.button.style href=(concat widgetConfig.button.link (utmVariables widgetConfig.button.utm)) width=widgetConfig.button.width}}
          {{widgetConfig.button.text}}
        {{/uiButton}}
      </td>
    </tr>
  {{/if}}
{{/table}}
